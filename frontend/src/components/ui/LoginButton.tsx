import React from "react";

const LoginButton: React.FC = () => {
  return (
    <a href="/api/login" className="bg-primary text-primary-foreground hover:bg-primary/90">Login</a>
  )
}

export default LoginButton
